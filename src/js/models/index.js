/**
 * Attach all model files here
 **/
define([
    './start-menu-items',
    './currencyPairs',
    './windowModels'
    ], function () {
    console.log("Declaring %d model(s)", arguments.length);
});
